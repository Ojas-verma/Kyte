# Firestore Rules Manual Check

Sprint 7 includes a demo-open Firestore rules file at `firestore.rules`.

Manual verification checklist:
- Deploy rules to Firebase project.
- Confirm `members` collection supports read/write for demo mode.
- Confirm app can add/edit/delete members when Firebase is connected.
